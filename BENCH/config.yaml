---
################################ IMPORTS #######################################
# Declare how to import sub-files from config.d
imports:
  - glob: config.d/hosts/*
    inject_at: hosts
  - glob: config.d/meta_variants/*
    inject_at: meta_variants
  - glob: config.d/variants/*
    inject_at: variants
  - glob: config.d/cases/*
    inject_at: cases
################################ TUNING ########################################
hosts:
  # if not host defined, fallback on this
  '@default':
    ressources:
      cpu: 8
      gpu: 1
    tuning:
      gnu: -march=native
      nvfortran: -tp=native
      mpi_options: ''

  # -- Here will be the others imported from config.d/hosts/* --

############################## DEFAULT SELECTION ###############################
default_selection:
  cases:
    - BASIN
    - BASIN-LARGE
  variants:
    - "@wip-all"
############################## META VARIANTS ###################################
meta_variants:

  # -- Here will be the others imported from config.d/meta_variants/* --

################################ VARIANTS ######################################
variants:

  # -- Here will be the others imported from config.d/variants/* --

############################## CASES ###########################################
cases:

  # -- Here will be the others imported from config.d/cases/* --
