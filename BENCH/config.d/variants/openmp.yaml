---
# openmp
openmp-{threads}:
  configure: --with-parallel=openmp --with-threads={threads}
  tuning_familly: gnu
  environ:
    OMP_NUM_THREADS: "{threads}"
  command_prefix: ''
  unpack:
    threads: [2, 4, 6, 8, 16, 32, 64]
  requires:
    cpu: "{threads}"
