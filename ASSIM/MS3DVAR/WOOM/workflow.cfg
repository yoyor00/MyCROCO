[app]
name=croco-ms3dvar
conf=wmed
exp=compile

[stages]

    [[prolog]]
    # step0: build mpc and generate headers from ms3dvar.cfg in parallel
    # step1: build all 4 variants in parallel once step0 is complete
    step0=compile_mpc,generate_headers
    step1=compile_filter,compile_lr,compile_mr,compile_ms
