[app]
name=ms3dvar
conf=assim
exp=compile

[stages]

    [[prolog]]
    # step0: build the shared mpc preprocessor first
    # step1: build all 4 variants in parallel once mpc is ready
    step0=compile_mpc
    step1=compile_filter,compile_lr,compile_mr,compile_ms
