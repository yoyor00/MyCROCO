# MS3DVAR build artifacts
# Executables (built in-place when running from source)
FILTER/msfilter
LR/mslr
MR/msmr
MS/msms
# Scale-specific Makefile.inc (generated by jobcomp in Compile/ now, but kept as safety)
*/Makefile.inc

# Legacy das_* symlinks (old name -> new ms3dvar_* name), regenerated on
# demand by TOOLS/create_legacy_symlinks.sh for diffing against the
# original (pre-rename) MS-3DVAR repo. Not part of the build.
*/das_*
